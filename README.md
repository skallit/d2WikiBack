Projet cour d2-wiki-back Pour lancer le projet prérequis:

    node.js

Cmd a faire:

    npm i

Lancer le projet:

    npm run start

Swagger can be accesible with adding /api to the base url of the api for exemple:

    http://localhost:3004/api
